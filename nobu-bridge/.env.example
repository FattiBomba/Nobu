# Nobu Bridge Environment Configuration
# Copy this file to .env and fill in your values

# ===========================================
# DEPLOYMENT MODE
# ===========================================
# cloud = Use cloud APIs (start here - requires API keys)
# local = All AI runs locally (recommended for privacy)
# hybrid = Cloud with local fallback
DEPLOYMENT_MODE=cloud

# ===========================================
# CLOUD LLM - GEMINI (when DEPLOYMENT_MODE=cloud)
# ===========================================
# Get your API key from: https://makersuite.google.com/app/apikey
GEMINI_API_KEY=your-gemini-api-key-here
GEMINI_MODEL=gemini-2.0-flash

# Alternative: OpenAI
# OPENAI_API_KEY=sk-your-key-here
# OPENAI_MODEL=gpt-4o-mini

# ===========================================
# CLOUD SPEECH-TO-TEXT (when DEPLOYMENT_MODE=cloud)
# ===========================================
STT_PROVIDER=google

# Google Cloud Speech-to-Text (uses same GEMINI_API_KEY or GOOGLE_API_KEY)
# Alternative: openai (uses OPENAI_API_KEY for Whisper)

# ===========================================
# CLOUD TEXT-TO-SPEECH (when DEPLOYMENT_MODE=cloud)
# ===========================================
TTS_PROVIDER=google

# Google Cloud Text-to-Speech
# Voice options: en-US-Neural2-F (female), en-US-Neural2-D (male)
GOOGLE_TTS_VOICE=en-US-Neural2-F

# Alternative: openai
# OPENAI_TTS_VOICE=nova

# ===========================================
# LOCAL LLM - OLLAMA (when DEPLOYMENT_MODE=local)
# ===========================================
# Install: curl -fsSL https://ollama.com/install.sh | sh
# Pull model: ollama pull gemma2:9b
OLLAMA_BASE_URL=http://localhost:11434
OLLAMA_MODEL=gemma2:9b

# Model options (choose based on your hardware):
# - gemma2:9b     (needs ~8GB RAM, great quality)
# - llama3.2:3b   (needs ~4GB RAM, fast)
# - mistral:7b    (needs ~6GB RAM, good conversation)

# ===========================================
# LOCAL SPEECH-TO-TEXT (when DEPLOYMENT_MODE=local)
# ===========================================
# STT_PROVIDER=faster-whisper
WHISPER_MODEL=small
WHISPER_DEVICE=cpu
WHISPER_COMPUTE_TYPE=int8

# ===========================================
# LOCAL TEXT-TO-SPEECH (when DEPLOYMENT_MODE=local)
# ===========================================
# TTS_PROVIDER=piper
PIPER_VOICE=en_US-amy-medium
PIPER_VOICE_PATH=./voices/
PIPER_SPEED=1.0

# ===========================================
# OPENCLAW INTEGRATION (optional)
# ===========================================
# If you want to use OpenClaw as the brain instead of direct LLM
USE_OPENCLAW=false
OPENCLAW_URL=http://localhost:3000

# ===========================================
# SERVER CONFIGURATION
# ===========================================
BRIDGE_HOST=0.0.0.0
BRIDGE_PORT=8765

# Maximum concurrent Nobu clients
MAX_CLIENTS=20

# ===========================================
# CLIENT MANAGEMENT
# ===========================================
# Enable auto-discovery of new Nobu devices
AUTO_DISCOVERY=true

# Require device registration before connecting
REQUIRE_REGISTRATION=false

# ===========================================
# LOGGING
# ===========================================
LOG_LEVEL=INFO
LOG_FILE=./logs/nobu-bridge.log
